package:
    name: py-test
    version: 1.0.0
build:
    skip:
      - env.get('CF_DPCPP_ENABLED') != "True"

requirements:
    build:
        - ${{ compiler('c') }}
        - ${{ compiler('dpcpp') }}
    host:
        - python
    run:
        - python
