package:
    name: py-test
    version: 1.0.0
build:
    skip: true  # [not win]
requirements:
    build:
        - {{ compiler('c') }}
    host:
        - python
    run:
        - python
about:
    home: home
