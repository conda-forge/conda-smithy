#!/usr/bin/env bash

# PLEASE NOTE: This script has been automatically generated by conda-smithy. Any changes here
# will be lost next time ``conda smithy rerender`` is run. If you would like to make permanent
# changes to this script, consider a proposal to conda-smithy so that other feedstocks can also
# benefit from the improvement.

set -xeuo pipefail
export PYTHONUNBUFFERED=1
export FEEDSTOCK_ROOT=/home/conda/feedstock_root
export CI_SUPPORT=/home/conda/feedstock_root/.ci_support
export utils=${FEEDSTOCK_ROOT}/.circleci/build_utils.py

cat >~/.condarc <<CONDARC

conda-build:
 root-dir: /home/conda/feedstock_root/build_artifacts

CONDARC

# set up the condarc
$utils setup_conda ${CI_SUPPORT}/${CONFIG}.yaml

# A lock sometimes occurs with incomplete builds. The lock file is stored in build_artifacts.
conda clean --lock

conda install --yes --quiet conda-forge::conda-forge-ci-setup=1 conda-build
{% if build_setup -%}
{{ build_setup }}{% endif -%}

# make the build number clobber
$utils make_build_number /home/conda/recipe_root "${CI_SUPPORT}/${CONFIG}.yaml"

conda build /home/conda/recipe_root -m "${CI_SUPPORT}/${CONFIG}.yaml" \
    --clobber-file "${CI_SUPPORT}/clobber_${CONFIG}.yaml"  --quiet

$utils upload_package /home/conda/recipe_root "${CI_SUPPORT}/${CONFIG}.yaml"

touch "/home/conda/feedstock_root/build_artifacts/conda-forge-build-done-${CONFIG}"
